<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        No permissions declared directly in the app shell. Each feature
        module (core-recording, later core-telephony) declares exactly the
        permissions and components it needs in its own manifest; the
        Android Gradle Plugin merges them into the final app manifest.
        This keeps "what needs what" traceable to the module that owns it.
    -->

    <application
        android:name=".CallManagerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallManager">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CallManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
